<html>
<head>
    <title>Shoe Stores</title>
</head>
<body>
    <h1>{{ brand.getBrandName }}</h1>

    {% if stores is not empty %}
        <p>Here are the stores where this brand is carried:</p>
        <ul>
            {% for store in stores %}
                <li><a href="/stores/{{ store.getId }}">{{ store.getStoreName }}</a></li>
                {% endfor %}

        </ul>
        {% else %}
        <p>No stores found for this brand! Add a store below.</p>

    {% endif %}

    <p>Add a new store for this brand:</p>
    <form action='/brand/{{ brand.getId }}/add_store' method='post'>

        <label for="store_name">Store name</label>
        <input id='store_name' name='store_name' type='text'>



        <button type='submit'>Add store</button>
    </form>

<a href="/">Return to stores page</a>





</body>
</html>
